build:
  docker:
    server:server/Dockerfile
    client:client/Dockerfile
  config:
    API_HOST: "http://localhost:3000/"
    APP_SERVER_PORT:8000
    REACT_APP_PORT:3000

release:
   server: yarn dev
   client: yarn start
